/* HUD::OnPendingDestroy() at 0x020fd5dc (ov002) -- vtable slot 12.
 * Empty override; the HUD does nothing when marked for destruction.
 */

struct HUD;

void _ZN3HUD16OnPendingDestroyEv(struct HUD *self)
{
    (void)self;
}
